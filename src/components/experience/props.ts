export interface XPCardProps {
  text: string;
  side: 'left' | 'right';
  startDate: string;
  endDate: string;
  company: string;
  position: string;
}

export interface BulaProps {
  startDate: string;
  endDate: string;
}

